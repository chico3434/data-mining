---
title: "clustering_depart"
output: pdf_document
---

# Clusterização 1: Atraso na Partida

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
library(tidyverse)
library(dplyr)
library(daltoolbox)
```

## Carregamento do dataframe

```{r bfd_load}
load("/home/matheus_melo/data-mining/bfd_2022.rdata")

cols <- c("expected_depart", "real_depart", "expected_arrival", "real_arrival")

for (col in cols) {
  bfd[[col]] <- as.POSIXct(bfd[[col]])
}

bfd
```

## Filtro com apenas as colunas numéricas 

```{r bfd_depart_numerical}

cols <- c("delay_depart", "depart_air_temperature", "depart_dew_point", "depart_relative_humidity", "depart_wind_direction", "depart_wind_speed", "depart_pressure", "depart_visibility", "depart_apparent_temperature")

bfd_depart_numerical <- bfd[, cols]

bfd_depart_numerical
```

## Remoção de nulos

```{r bfd_depart_numerical_removal}

quantidade_nulos <- bfd_depart_numerical %>%
  summarise(across(everything(), ~ sum(is.na(.)))) %>%
  pivot_longer(cols = everything(), names_to = "coluna", values_to = "qtd_na") %>%
  arrange(desc(qtd_na))

quantidade_nulos
```

```{r bfd_depart_numerical_na_omit}

bfd_depart_numerical <- na.omit(bfd_depart_numerical)

bfd_depart_numerical
```


## Remoção de outliers

```{r bfd_depart_numerical_outliers_removal}

out_obj <- outliers_boxplot(alpha = 3) # class for outlier analysis
out_obj <- fit(out_obj, bfd_depart_numerical) # computing boundaries
bfd_depart_numerical <- transform(out_obj, bfd_depart_numerical) # returning cleaned dataset

# inspection of cleaned dataset
bfd_depart_numerical
```

## Normalização dos dados

```{r bfd_depart_numerical_normalization}

# Z-Score
# Adjust values to 0 (mean), 1 (variance).

norm <- zscore()
norm <- fit(norm, bfd_depart_numerical)
bfd_depart_numerical <- transform(norm, bfd_depart_numerical)
bfd_depart_numerical
```

## Busca do melhor K do Kmeans

```{r bfd_depart_numerical_clustering}

set.seed(123)

tempo_exec <- system.time({

  # Seu dataset numérico já tratado
  dados <- bfd_depart_numerical

  # Faixa de valores de k a testar
  k_values <- 1:8
  wcss_dal <- numeric(length(k_values))
  wcss_trad <- numeric(length(k_values))

  # Loop para calcular o WCSS para cada k
  for (i in seq_along(k_values)) {
    k <- k_values[i]

    # daltoolbox
    model <- cluster_kmeans(k = k)
    clu <- cluster(model, dados)
    wcss_dal[i] <- attr(clu, "metric")  # métrica da daltoolbox

    # tradicional (stats::kmeans)
    km <- kmeans(dados, centers = k)
    wcss_trad[i] <- km$tot.withinss       # WCSS clássico
  }

  # Plots lado a lado
  par(mfrow=c(1,2)) # 2 gráficos na mesma janela

  plot(k_values, wcss_dal, type = "b", pch = 19, frame = FALSE,
       xlab = "Número de clusters (k)", ylab = "Métrica daltoolbox",
       main = "daltoolbox (crescente)")

  plot(k_values, wcss_trad, type = "b", pch = 19, frame = FALSE,
       xlab = "Número de clusters (k)", ylab = "WCSS tradicional",
       main = "stats::kmeans (decrescente)")

  # Mostrar os valores
  print("Métrica daltoolbox:")
  print(wcss_dal)
  print("WCSS tradicional:")
  print(wcss_trad)

})

# Tempo de execução
print(paste("Tempo de execução:", tempo_exec["elapsed"], "segundos"))
```

## Criação do Dataframe final para análise dos dados de partida

```{r bfd_depart_numerical_clustered}

set.seed(123)

# Definir o modelo k-means com k=4, visto que foi o melhor k definido
model <- cluster_kmeans(k = 4)

# Rodar o clustering nos seus dados numéricos
clu <- cluster(model, bfd_depart_numerical)

# Reverte a normalização dos dados para o estado original
bfd_depart_numerical <- inverse_transform(norm, bfd_depart_numerical)

# Selecionar as linhas do dataset original
bfd_depart_final <- bfd[rownames(bfd_depart_numerical), c("arrival", "depart", "route", "company", "flight", "di", "type", "depart_day_period", "expected_depart", "real_depart", "status_depart", "delay_depart", "depart_air_temperature", "depart_dew_point", "depart_relative_humidity", "depart_wind_direction", "depart_sky_coverage", "depart_pressure", "depart_visibility", "depart_apparent_temperature", "depart_wind_speed_scale", "depart_wind_direction_cat")]

# Adicionar a coluna de clusters
bfd_depart_final$clusters <- clu

bfd_depart_final
```
```{r save_results}

# Definir caminho do arquivo
caminho_saida <- "/home/matheus_melo/data-mining/T2/clustering_depart/bfd_depart_final.RData"

# Salvar o objeto bfd_depart_final
save(bfd_depart_final, file = caminho_saida)

# Mensagem de confirmação
cat("Arquivo RData salvo em:", caminho_saida, "\n")

```

